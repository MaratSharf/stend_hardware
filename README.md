# Стенд машинного зрения

Автоматизированная система контроля качества деталей на основе промышленной камеры Hikrobot и контроллера ОВЕН.

**Версия:** 4.5.1  
**Последнее обновление:** 20 мая 2026

## 📋 Содержание

- [Назначение](#назначение)
- [Оборудование](#оборудование)
- [Принцип работы](#принцип-работы)
- [Режимы работы](#режимы-работы)
- [Установка и запуск](#установка-и-запуск)
- [Конфигурация](#конфигурация)
- [Веб-интерфейс](#веб-интерфейс)
- [Авторизация и роли](#авторизация-и-роли)
- [База данных](#база-данных)
- [Структура проекта](#структура-проекта)
- [Логирование](#логирование)
- [Безопасность](#безопасность)
- [Диагностика](#диагностика)

---

## Назначение

Система предназначена для автоматического визуального контроля деталей на конвейере:

1. **Обнаружение детали** — датчики фиксируют появление объекта.
2. **Позиционирование** — деталь останавливается под камерой.
3. **Съёмка и анализ** — камера делает снимок и определяет качество.
4. **Сортировка** — деталь перемещается к выходу, результат индицируется лампами.
5. **Сохранение результатов** — все проверки записываются в базу данных.

**Результаты:**
- ✅ **OK (годен)** — зелёная лампа, деталь движется к датчику Д4.
- ❌ **NG (брак)** — красная лампа, деталь движется к датчику Д3.

### Поддерживаемые инструменты Hikrobot

Система поддерживает **50+ инструментов машинного зрения** Hikrobot.  
Список инструментов хранится в таблице `tools` базы данных.  
**Особенности:**
- Для инструмента **3.2 CodeRecognition** автоматически отображаются QR-код или OCR-текст.
- Каждый инструмент сопоставлен с проектом камеры (`project_name`), переключение происходит по команде Modbus.
- На странице "Инструменты" вкладка "Все" показывает до 10 последних использованных проектов.

---

## Оборудование

| Устройство | Модель | Назначение |
|------------|--------|------------|
| Камера машинного зрения | Hikrobot MV-SC3016-06M-WBN | Съёмка и анализ деталей |
| Контроллер ввода-вывода | ОВЕН МК210-312 | Подключение датчиков и исполнительных механизмов |
| Датчики | 6 шт. (Д1-Д4 + 2 тумблера) | Нормально замкнутые (NC) |
| Исполнительные механизмы | 4 выхода | Конвейер, толкатель (резерв), лампы OK/NG |

### Схема подключений ОВЕН

**Входы (DI):**
- DI0 → Датчик Д1 (вход конвейера)
- DI1 → Датчик Д2 (под камерой левый)
- DI2 → Датчик Д3 (под камерой правый)
- DI3 → Датчик Д4 (выход конвейера)
- DI4 → Тумблер (положение A)
- DI5 → Тумблер (положение B)

**Выходы (DO):**
- DO0 → Конвейер (пуск/стоп)
- DO1 → Толкатель брака (резерв)
- DO2 → Лампа NG (красная)
- DO3 → Лампа OK (зелёная)

### Сетевые настройки по умолчанию

| Устройство | IP-адрес | Порт |
|------------|----------|------|
| ОВЕН МК210 | 192.168.1.99 | 502 |
| Камера Hikrobot | 192.168.1.36 | 502 |

---

## Принцип работы

### Конечный автомат сценария C

IDLE → (спад Д1) → CONVEYOR_RUNNING_TO_CAMERA → (спад Д2) → POSITIONED
→ (съёмка) → CONVEYOR_RUNNING_TO_EXIT → (спад Д3/Д4) → IDLE

### Выбор сценария

| Тумблер A | Тумблер B | Сценарий |
|-----------|-----------|----------|
| 1 | 0 | A (непрерывный контроль) |
| 0 | 1 | B (одиночный снимок) |
| 0 | 0 | C (циклический контроль) |

Также возможен принудительный выбор через веб-интерфейс.

---

## Сценарии работы

Система поддерживает три сценария, переключаемых тумблерами на ОВЕН или через веб-интерфейс.

### Сценарий C — Циклический контроль (по умолчанию)

Автоматический цикл инспекции одной детали.

**Конечный автомат:**

```
IDLE → CONVEYOR_RUNNING_TO_CAMERA → POSITIONED → CONVEYOR_RUNNING_TO_EXIT → IDLE
```

**Детальное описание состояний:**

| Состояние | Конвейер | Лампа OK | Лампа NG | Описание |
|-----------|----------|----------|----------|----------|
| `IDLE` | ⛔ Выкл | ✅ Вкл | ⛔ Выкл | Ожидание детали на входе (Д1) |
| `CONVEYOR_RUNNING_TO_CAMERA` | ✅ Вкл | ⛔ Выкл | ⛔ Выкл | Конвейер запущен, деталь движется к камере |
| `POSITIONED` | ⛔ Выкл | ⛔ Выкл | ⛔ Выкл | Деталь под камерой, ожидание измерения |
| `CONVEYOR_RUNNING_TO_EXIT` | ✅ Вкл | ✅/❌ | ❌/✅ | Деталь движется к выходу после измерения |

**Триггеры переходов:**

1. **IDLE → CONVEYOR_RUNNING_TO_CAMERA** — спад сигнала на Д1 (DI0, датчик входа). Конвейер запускается.
2. **CONVEYOR_RUNNING_TO_CAMERA → POSITIONED** — спад сигнала на Д2 (DI1, датчик под камерой левый). Конвейер останавливается.
3. **POSITIONED → CONVEYOR_RUNNING_TO_EXIT** — автоматический запуск измерения камеры.
   - Результат **OK** → зелёная лампа, цель — Д4 (DI3, выход конвейера).
   - Результат **NG** → красная лампа, цель — Д3 (DI2, датчик под камерой правый).
4. **CONVEYOR_RUNNING_TO_EXIT → IDLE** — спад сигнала на целевом датчике. Цикл повторяется.

**Таймаут:** Если цикл не завершается за `state_timeout` секунд (по умолчанию 30), срабатывает ошибка — все выходы сбрасываются, мигает красная лампа.

**Требования:** Должен быть выбран инструмент (проект камеры) в `config.yaml` или через веб-интерфейс.

---

### Сценарий A — Непрерывный контроль

Периодическая инспекция без конвейера (деталь фиксируется под камерой вручную).

**Поведение:**

1. При активации запускается зелёная лампа, конвейер включается.
2. Камера делает измерение каждые `scenario_a_interval` секунд (по умолчанию 0.5 с).
3. Логика остановки по результату:
   - **OK** → зелёная ламга гаснет, ждём следующий результат.
   - **NG (первый)** → красная лампа горит, конвейер продолжает работать.
   - **NG (второй подряд после OK или NG)** → остановка: конвейер выключается, красная лампа горит постоянно, сценарий деактивируется.
   - **null/невалидный результат** (после валидных) → сброс, зелёная лампа гаснет.

**Применение:** Контроль деталей, зафиксированных под камерой, с периодической проверкой качества.

---

### Сценарий B — Одиночный снимок

Инспекция по нажатию тумблера B (DI5).

**Поведение:**

1. Ожидание фронта сигнала на DI5 (нажатие тумблера B).
2. При обнаружении фронта — мигание зелёной лампы (1 сек).
3. Камера делает одно измерение.
4. Результат:
   - **OK** → зелёная лампа горит 2 сек.
   - **NG** → красная лампа горит 2 сек.
   - **Ошибка** → мигание красной лампы (1.5 сек, 2 Гц).

**Применение:** Ручная проверка отдельных деталей по команде оператора.

---

### Переключение сценариев

**Через тумблеры на ОВЕН:**

| Тумблер A (DI4) | Тумблер B (DI5) | Сценарий |
|-----------------|-----------------|----------|
| 1 | 0 | A |
| 0 | 1 | B |
| 0 | 0 | C |
| 1 | 1 | Недействительно (игнорируется) |

Переключение подтверждается через 3 секунды удержания положения.

**Через веб-интерфейс:**

1. На странице «Настройки» включите «Выбор сценария из веб».
2. Выберите сценарий (A, B или C) в выпадающем списке.
3. Нажмите «Запустить» для активации.

При переключении сценария все выходы сохраняют текущее состояние до тех пор, пока новый сценарий не установит свои значения.

---

## Режимы работы

🟢 **Автоматический режим** (по умолчанию) – работа по заданному алгоритму.

🟡 **Ручной режим** – автоматический алгоритм останавливается, доступно ручное управление через страницу `/debug`.

🔧 **Офлайн-режим** – чекбокс «Офлайн» на главной странице отключает попытки связи с оборудованием (демо-режим). В этом режиме уведомления о потере связи не показываются.

---

## Установка и запуск

### Требования

- Python 3.8+
- PostgreSQL
- Сетевой доступ к оборудованию

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка базы данных PostgreSQL

Создайте базу данных и пользователя, укажите параметры в `config.yaml`:
```yaml
database:
  type: postgresql
  host: localhost
  port: 5432
  dbname: stend_db
  user: stend_user
  password: your_password
```

### Запуск приложения
```bash
python run.py
```

После запуска:
- **Веб-интерфейс:** http://0.0.0.0:5001
- **Логи:** `data/logs/`
- **Изображения:** `data/images/`

---

## Конфигурация

Файл `config.yaml` содержит все настройки. Основные параметры:

```yaml
owen:
  ip: 192.168.1.99
  port: 502
  unit: 1
  timeout: 1.0

camera:
  ip: 192.168.1.36
  port: 502
  unit: 1
  control_offset: 0
  status_offset: 1
  result_offset: 2
  command_offset: 500
  byte_order: 'little'
  project_name: '6.2Fixture'
  scenario_a_interval: 0.5

controller:
  cycle_time: 0.2
  debounce_ms: 10
  camera_ready_interval: 10
  ejector_pulse: 0.5
  state_timeout: 30.0

paths:
  images: data/images
  logs: data/logs
```

---

## Веб-интерфейс

Интерфейс выполнен в стиле vdt-service.ru (синяя гамма, шрифт Inter).

| URL | Описание |
|-----|----------|
| `/` | Главная панель мониторинга |
| `/tools` | Каталог инструментов Hikrobot из БД (с избранным) |
| `/history` | История проверок с фильтрацией и пагинацией |
| `/debug` | Ручное управление выходами и триггер камеры |
| `/settings` | Редактирование конфигурации, резервные копии |
| `/reports` | Формирование отчётов (дневных, сменных и т.д.) |
| `/users` | Управление пользователями (только администраторы) |

### Элементы интерфейса

Все страницы содержат:
- **Сайдбар** с навигацией и блоком текущего пользователя
- **Кнопку выхода** (🚪) в правом верхнем углу
- **Ссылку "Пользователи"** (👥) в сайдбаре (видна только администраторам)

---

## Авторизация и роли

Система требует аутентификации для доступа ко всем страницам.

### Роли пользователей

| Роль | Описание | Доступ |
|------|----------|--------|
| **admin** | Администратор | Полный доступ ко всем страницам, включая управление пользователями |
| **naladchik** | Наладчик | Доступ ко всем страницам, кроме управления пользователями |
| **kontroler** | Контролер | Мониторинг, история, отчёты |
| **operator** | Оператор | Мониторинг, история |

### Управление пользователями

Доступно только администраторам на странице `/users`:
- Добавление новых пользователей
- Редактирование данных и ролей
- Удаление пользователей
- Блокировка/активация учётных записей

### Стандартная учётная запись

После первой установки создайте администратора через `create_admin.py`:
```bash
python create_admin.py
```

---

## База данных

**Тип:** PostgreSQL

**Таблицы:**
- `inspection_results` — результаты проверок.
- `daily_stats` — агрегированная дневная статистика.
- `tools` — справочник инструментов Hikrobot.
- `production_orders` — заказы (заготовка для MES).
- `users` — учётные записи пользователей.

Структура таблиц создаётся автоматически при первом подключении.

---

## Структура проекта

```
stend_hardware/
├── run.py                     # Точка входа
├── config.yaml                # Конфигурация
├── requirements.txt           # Зависимости
├── README.md                  # Документация
├── KODA.md                    # Контекст для ИИ-агентов
├── create_admin.py            # Создание администратора
│
├── core/
│   ├── __init__.py
│   ├── config.py              # Загрузка конфигурации
│   └── controller.py          # Конечный автомат, логика сценариев
│
├── hardware/
│   ├── __init__.py
│   ├── owen.py                # Драйвер ОВЕН (Modbus TCP)
│   └── hikrobot.py            # Драйвер камеры Hikrobot (Modbus TCP)
│
├── web/
│   ├── __init__.py
│   ├── app.py                 # Фабрика Flask-приложения
│   ├── pages/                 # Blueprints страниц
│   │   ├── auth.py            # Авторизация/аутентификация
│   │   ├── monitoring.py      # Главная страница
│   │   ├── tools.py           # Каталог инструментов
│   │   ├── history.py         # История проверок
│   │   ├── debug.py           # Отладка
│   │   ├── settings.py        # Настройки
│   │   ├── reports.py         # Отчёты
│   │   └── users.py           # Управление пользователями
│   ├── static/                # CSS, JS
│   │   ├── css/               # Стили страниц
│   │   ├── js/                # Клиентские скрипты
│   │   └── images/            # Логотипы
│   └── templates/             # HTML-шаблоны
│
├── utils/
│   ├── __init__.py
│   ├── logger.py              # Логирование
│   ├── database.py            # Работа с PostgreSQL
│   ├── config_manager.py      # Управление config.yaml
│   ├── report_generator.py    # Формирование отчётов
│   ├── excel_export.py        # Экспорт отчётов в Excel
│   └── pdf_export.py          # Экспорт отчётов в PDF
│
└── data/                      # Создаётся при работе
    ├── images/                # Снимки (OK/NG)
    │   ├── OK/
    │   └── NG/
    ├── logs/                  # Файлы логов
    └── backups/               # Резервные копии конфигурации
```

---

## Логирование

Логи сохраняются в `data/logs/`:
- `controller.log` — работа контроллера.
- `owen.log` — Modbus-обмен с ОВЕН.
- `hikrobot.log` — Modbus-обмен с камерой.
- `web.log` — запросы веб-сервера.
- `test.log` — отладочная информация.

Ротация: при достижении 10 МБ создаётся новый файл (до 5 копий).

---

## Безопасность

- **XSS-защита:** экранирование данных в DOM.
- **Валидация входных данных** (IP, имя проекта).
- **Клиентские ошибки** отправляются на сервер через `/api/client_error`.
- **Пароль БД** хранится в `config.yaml` (рекомендуется использовать переменные окружения).
- **Авторизация:** все страницы защищены, доступ по ролям.
- **Хеширование паролей:** bcrypt.

---

## Диагностика

### Проверка подключения
```bash
ping 192.168.1.99   # ОВЕН
ping 192.168.1.36   # камера
```

### Просмотр логов
```bash
tail -f data/logs/controller.log
```

### Типовые проблемы

| Проблема | Возможная причина | Решение |
|----------|-------------------|---------|
| Нет инструментов на /tools | Таблица tools пуста | Выполнить SQL-скрипт для заполнения таблицы |
| Ошибка подключения к БД | Неверные параметры в config.yaml | Проверить хост, порт, логин, пароль |
| Изображения не отображаются | Папка data/images недоступна | Проверить права и путь в конфиге |
| Сообщения о потере связи не появляются | Офлайн-режим включён | Отключить чекбокс «Офлайн» |
| Ошибка `is_xhr` в Flask | Устаревший код | Обновить `auth.py` (использовать `request.headers.get('X-Requested-With')`) |

---

## API эндпоинты (основные)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/status` | Последний результат инспекции |
| GET | `/api/hardware_status` | Состояние входов/выходов, статус камеры |
| POST | `/api/switch_project` | Переключение проекта камеры |
| GET | `/api/tools` | Список инструментов из БД |
| GET | `/api/tools/<tool_id>` | Детали инструмента |
| POST | `/api/tools/<tool_id>/favorite` | Добавить/убрать из избранного |
| GET | `/api/tools/favorites` | Список избранных инструментов |
| GET | `/api/recent_tools` | Последние 10 использованных проектов |
| GET | `/api/results` | История результатов (с фильтрами) |
| GET | `/api/results/<id>` | Детали одной проверки |
| GET | `/api/statistics` | Общая статистика |
| GET/POST | `/api/mode` | Управление режимом (авто/ручной) |
| POST | `/api/offline_mode` | Включение/выключение офлайн-режима |
| GET | `/api/connection_status` | Статус связи с оборудованием |
| GET | `/api/export/csv` | Экспорт истории в CSV |
| GET | `/api/export/excel` | Экспорт истории в Excel |
| GET | `/api/export/pdf` | Экспорт истории в PDF |
| GET | `/api/users` | Список пользователей (admin) |
| POST | `/api/users` | Создание пользователя (admin) |
| PUT | `/api/users/<id>` | Обновление пользователя (admin) |
| DELETE | `/api/users/<id>` | Удаление пользователя (admin) |

---

## Заключение

Проект представляет собой полнофункциональный стенд машинного зрения с современным веб-интерфейсом, модульной серверной архитектурой, базой данных и системой авторизации. Система готова к эксплуатации.
npx @agent-cli/agent-cli@latest