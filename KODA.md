# KODA.md — Контекст проекта MV Service v4.6.0

## Описание проекта

**Стенд машинного зрения (MV Service)** — автоматизированная система контроля качества деталей на основе промышленной камеры Hikrobot и контроллера ОВЕН.

**Версия:** 4.6.0  
**Последнее обновление:** 9 июля 2026

## Запуск

```bash
python run.py
```

**Веб-интерфейс:** http://localhost:5001

## База данных

- **Тип:** PostgreSQL
- **База:** `stend_hardware`
- **Пользователь:** `stend`
- **Файл конфигурации:** `config.yaml`
- **Секретный ключ сессий:** `config.yaml → web.secret_key`

## Сценарии работы

### Сценарий A — Непрерывный контроль
Периодическая инспекция с конвейером. Лампы включаются только по результату (OK → зелёная, NG → красная).

### Сценарий B — Одиночный снимок
Фронт DI5 → ожидание фронта DI6 → триггер камеры → результат (OK/NG).

### Сценарий C — Циклический контроль
Конечный автомат: IDLE → CONVEYOR_RUNNING_TO_CAMERA → POSITIONED → CONVEYOR_RUNNING_TO_EXIT → IDLE.
Лампы включаются только по результату измерения.

### Лампы
Лампы (DO2 красная, DO3 зелёная) включаются **только** по результату инспекции.
Во всех остальных случаях (простой, ошибка камеры, ожидание) обе лампы выключены.

### Flask 2.2+ совместимость

В `web/pages/auth.py` заменено:
- `request.is_xhr` → `request.headers.get('X-Requested-With')`

Изменения в 3 местах:
1. Проверка AJAX-запросов
2. Обработка редиректов
3. Ответы на клиент

## Авторизация и роли

### Роли пользователей

| Роль | Права |
|------|-------|
| `admin` | Полный доступ, включая управление пользователями |
| `naladchik` | Доступ ко всем страницам, кроме управления пользователями |
| `kontroler` | Мониторинг, история, отчёты |
| `operator` | Мониторинг, история |

### Элементы интерфейса авторизации

Все страницы содержат:
- **Сайдбар** с навигацией и блоком текущего пользователя
- **Ссылку "Пользователи"** (👥) в сайдбаре (видна только `admin`)
- **Кнопку выхода** (🚪) в сайдбаре и в правом верхнем углу

### Файлы авторизации

- `web/pages/auth.py` — Flask-маршруты авторизации
- `web/pages/users.py` — управление пользователями
- `web/static/js/auth.js` — клиентская логика (редирект на /auth/login, показ элементов)
- `web/templates/login.html` — страница входа
- `web/templates/_sidebar.html` — общий сайдбар с элементами авторизации

## Структура проекта

```
stend_hardware/
├── run.py                     # Точка входа
├── config.yaml                # Конфигурация
├── requirements.txt           # Зависимости
├── README.md                  # Документация
├── KODA.md                    # Этот файл
├── create_admin.py            # Создание администратора
│
├── core/
│   ├── config.py              # Загрузка конфигурации
│   └── controller.py          # Конечный автомат, логика сценариев
│
├── hardware/
│   ├── owen.py                # Драйвер ОВЕН (Modbus TCP)
│   └── hikrobot.py            # Драйвер камеры Hikrobot (Modbus TCP)
│
├── web/
│   ├── app.py                 # Фабрика Flask-приложения
│   ├── pages/                 # Blueprints страниц
│   │   ├── auth.py            # Авторизация/аутентификация
│   │   ├── monitoring.py      # Главная страница
│   │   ├── tools.py           # Каталог инструментов
│   │   ├── history.py         # История проверок
│   │   ├── debug.py           # Отладка
│   │   ├── settings.py        # Настройки
│   │   ├── reports.py         # Отчёты
│   │   └── users.py           # Управление пользователями
│   ├── static/
│   │   ├── css/               # Стили страниц
│   │   └── js/                # Клиентские скрипты
│   └── templates/
│       ├── _sidebar.html      # Общий сайдбар
│       ├── login.html         # Страница входа
│       └── *.html             # Страницы приложений
│
├── utils/
│   ├── logger.py              # Логирование
│   ├── database.py            # Работа с PostgreSQL
│   ├── config_manager.py      # Управление config.yaml
│   ├── report_generator.py    # Формирование отчётов
│   └── excel_export.py        # Экспорт отчётов в Excel
│
└── data/
    ├── images/                # Снимки (OK/NG)
    ├── logs/                  # Файлы логов
    └── backups/               # Резервные копии конфигурации
```

## Основные API

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/status` | Последний результат инспекции |
| GET | `/api/hardware_status` | Состояние входов/выходов |
| POST | `/api/switch_project` | Переключение проекта камеры |
| GET | `/api/tools` | Список инструментов из БД |
| GET | `/api/tools/<tool_id>` | Детали инструмента |
| POST | `/api/tools/<tool_id>/favorite` | Добавить/убрать из избранного |
| GET | `/api/tools/favorites` | Список избранных инструментов |
| GET | `/api/recent_tools` | Последние 10 использованных проектов |
| GET | `/api/results` | История результатов |
| GET | `/api/results/<id>` | Детали одной проверки |
| GET | `/api/statistics` | Общая статистика |
| GET/POST | `/api/mode` | Управление режимом (авто/ручной) |
| POST | `/api/offline_mode` | Включение/выключение офлайн-режима |
| GET | `/api/connection_status` | Статус связи с оборудованием |
| GET | `/api/export/csv` | Экспорт истории в CSV |
| GET | `/api/export/excel` | Экспорт истории в Excel |
| GET | `/api/export/pdf` | Экспорт истории в PDF |
| GET | `/api/users` | Список пользователей (admin) |
| POST | `/api/users` | Создание пользователя (admin) |
| PUT | `/api/users/<id>` | Обновление пользователя (admin) |
| DELETE | `/api/users/<id>` | Удаление пользователя (admin) |

## Стандартные учётные данные

После первого запуска выполните:
```bash
python create_admin.py
```

## Сетевые настройки

| Устройство | IP-адрес | Порт |
|------------|----------|------|
| ОВЕН МК210 | 192.168.1.99 | 502 |
| Камера Hikrobot | 192.168.1.36 | 502 |

## Логирование

- `data/logs/controller.log` — работа контроллера
- `data/logs/owen.log` — Modbus-обмен с ОВЕН
- `data/logs/hikrobot.log` — Modbus-обмен с камерой
- `data/logs/web.log` — запросы веб-сервера

## Безопасность

- XSS-защита (экранирование в DOM)
- Валидация входных данных
- Хеширование паролей (bcrypt)
- Авторизация по ролям

## Примечания для ИИ-агентов

1. **Не предполагайте библиотеки** — проверяйте `requirements.txt` и импорты.
2. **Соблюдайте соглашения** — имена, стили, структура кода.
3. **Тестируйте изменения** — запускайте `python run.py` и проверяйте веб-интерфейс.
4. **Комментируйте редко** — только для сложной логики.
5. **Не пишите болтовню** — краткие, прямые ответы.
